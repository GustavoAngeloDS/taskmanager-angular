import { NotificationConfiguration } from './notification-configuration.model';

describe('NotificationConfiguration', () => {
  it('should create an instance', () => {
    expect(new NotificationConfiguration()).toBeTruthy();
  });
});
